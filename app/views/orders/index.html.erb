<div class="container-fluid">
  <div class="row">
    <% @orders.each do |order| %>
      <div class="col-sm">
        <h2>
          Order # <%= order.id %>:
          <%= link_to order.product.name, order %>
        </h2>
        <p><%= order.product.description %></p>
        <strong><%= order.product.price.to_i %> Kč</strong>
      </div><!-- /col -->
    <% end %>
  </div>
</div>
